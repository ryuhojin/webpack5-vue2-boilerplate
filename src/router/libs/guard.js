import router from "..";

/**
 * @name router.beforeEach
 * @description 라우터 변경 전처리
 */
router.beforeEach((to, from, next) => {});


/**
 * @name router.afterEach
 * @description 라우터 변경 후처리 
 */
router.afterEach((to, from) => {});
